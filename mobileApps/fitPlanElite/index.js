import { AppRegistry } from 'react-native';
import App from './App'; // Your main app component file

AppRegistry.registerComponent('fitPlanElite', () => App); // Make sure the app name matches your project name
